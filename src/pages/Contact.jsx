import { useState } from "react";
import { SubHero, Footer } from "../components/Section.jsx";
export default function Contact({ t }) { const [msg,setMsg]=useState(""); const submit=(e)=>{e.preventDefault(); const data=new FormData(e.currentTarget); const body=encodeURIComponent(`名前: ${data.get('name')}\nメールアドレス: ${data.get('email')}\n\n問い合わせ内容:\n${data.get('message')}`); window.location.href=`mailto:aec.official@outlook.jp?subject=${encodeURIComponent('AEC Project お問い合わせ')}&body=${body}`}; return <><SubHero kicker="contact home" title="CONTACT" lead={t.pages.contactLead}/><section className="content section"><div className="contact-wrap"><div className="contact-panel reveal"><p className="kicker">{t.contact.gateway}</p><strong className="contact-mail">aec.official@outlook.jp</strong><p>{t.contact.note}</p></div><form className="contact-form reveal" onSubmit={submit}><div className="field"><label htmlFor="name">{t.contact.name}</label><input id="name" name="name" required placeholder={t.contact.namePlaceholder}/></div><div className="field"><label htmlFor="email">{t.contact.email}</label><input id="email" name="email" type="email" required placeholder="example@example.com"/></div><div className="field"><label htmlFor="message">{t.contact.message}</label><textarea id="message" name="message" maxLength="1200" required value={msg} onChange={e=>setMsg(e.target.value)} placeholder={t.contact.messagePlaceholder}/><div className="counter">{msg.length} / 1200</div></div><button className="btn btn-primary" type="submit">{t.contact.submit}</button></form></div></section><Footer/></> }
